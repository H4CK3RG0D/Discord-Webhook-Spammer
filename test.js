const config = require('./config.json');

// console logs the url2 from config.json
console.log(config.url[0].url1);

// console logs the username from config.json
console.log(config.profile[0].avatarURL);